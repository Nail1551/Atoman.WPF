<Window x:Class="Atoman.WPF.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Atoman.WPF.Views"
        mc:Ignorable="d"
        Title="LoginView"  Height="300" Width="400" AllowsTransparency="True"  Background="Transparent"  WindowStyle="None" WindowStartupLocation="CenterScreen">
    <Border Background="Maroon" CornerRadius="30"  >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Логин:" Grid.Row="0" Margin="10"/>
        <TextBox x:Name="logbox" Grid.Row="1" Margin="10" Text="{Binding UserLogin}"/>

        <TextBlock Text="Пароль:" Grid.Row="2" Margin="10"/>
        <PasswordBox x:Name="pasbox" Grid.Row="3" Margin="10"/>

        <Button Content="Войти" Click="LoginButton_Click" Grid.Row="4" Margin="10"/>

    </Grid>
    </Border>
</Window>
